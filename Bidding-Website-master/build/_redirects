# Redirects for Horn of Antiques - React SPA routing
# This file ensures that all routes are handled by React Router

# API routes should not be handled by the frontend
/api/* 404

# All other routes should serve the React app
/* /index.html 200

# Legacy route redirects (if any old URLs need to be supported)
/auctions /
/browse /
/items /
